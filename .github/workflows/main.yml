name: Build and Push Docker Image
on:
  push:
    branches: ["buildjet"]
jobs:
  docker:
    runs-on: runnex-2vcpu-ubuntu-2404
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Check
        run: |
          lscpu
          curl ipinfo.io
          free -h
          df -h
