on: [push]
jobs:
  setup-docker:
    runs-on: macos-latest-xl
    name: test
    steps:
      - name: Setup Docker
        uses: douglascamata/setup-docker-macos-action@main
      - name: Run Docker
        uses: kohlerdominik/docker-run-action@main
        with: 
          image: diazaji/debian:clang
          volumes: ${{ github.workspace }}:/workspace
          run: |
            lscpu
            ls
            nproc
