workflows:
  kernel-compile:
    name: Kernel Compile
    instance_type: mac_mini_m1
    max_build_duration: 120
    scripts:
      - |
        while :; do
          echo loop
          sleep 1
        done
